layer.spotify = function(){
  layer.apply(this, arguments);
  // init spotify api shtuff
  this.init_();
};
$.inherits(layer.spotify, layer);

layer.spotify.prototype.init_ = function() {
  // todo
};

layer.spotify.prototype.decorate = function(parent) {
  // todo
};
